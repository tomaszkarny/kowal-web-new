import React from 'react'
import { SectionDividerWrapper } from './SectionDivider.styles'

/**
 * A simple divider component that creates space between sections
 */
export const SectionDivider = () => {
  return (
    <SectionDividerWrapper />
  )
}