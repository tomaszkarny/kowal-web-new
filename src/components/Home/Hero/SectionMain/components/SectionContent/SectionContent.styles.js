import styled from '@emotion/styled'
import { THEME } from 'consts/theme'

// These styles are just placeholders - in the final implementation,
// we'll move the relevant styles from SectionMain.styles.js to here

export const SectionContentWrapper = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  padding: 2rem 0;
`