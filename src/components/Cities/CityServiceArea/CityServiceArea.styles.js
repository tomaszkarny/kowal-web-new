import styled from '@emotion/styled'
import { FORGE_COLORS } from '../styles'

export const TagsGrid = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
`

export const RadiusNote = styled.p`
  margin-top: 1.5rem;
  color: ${FORGE_COLORS.textSecondary};
`
