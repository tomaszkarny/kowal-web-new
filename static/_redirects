# TEMPORARY MINIMAL REDIRECTS FILE TO FIX REDIRECT LOOP
# Only keeping one essential rule

# Redirect www to non-www (HTTPS only)
https://www.kowalstwo-karny.pl/* https://kowalstwo-karny.pl/:splat 301!

# All other redirects have been temporarily removed until redirect loop is fixed
